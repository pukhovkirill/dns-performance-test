# dns-performance-test

A high-performance, multithreaded DNS benchmarking tool written in C for Linux. It generates a stable, uniform stream of DNS requests to test server capacity under high QPS (queries per second) loads without overwhelming the network stack.